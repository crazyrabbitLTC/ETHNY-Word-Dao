pragma solidity ^0.5.0;

contract Registry {
  
  /*

    map from int -> string

    addWord(string word) returns integer
      - only adds if word doesn't exist
      - otherwise, error

    getWord(int key) returns string or null

  */
}