pragma solidity ^0.5.0;
//import "openzeppelin-eth/contracts/token/StandaloneERC20.sol"

contract Manager {
  /*

    addWord(word)
      if word isn't there, adds it
      issues token to

    getWord(word) returns int
      charges rent
      returns words

  */
}